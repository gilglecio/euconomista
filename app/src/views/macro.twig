{% if error|length %}
<div class="alert alert-danger">
    {% for e in error %}
    <span>{{ e }}</span>
    {% endfor %}
</div>
{% endif %}

{% if success|length %}
<div class="alert alert-success">
    {% for s in success %}
    <span>{{ s }}</span>
    {% endfor %}
</div>
{% endif %}